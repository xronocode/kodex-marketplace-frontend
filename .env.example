# Copy to .env and fill in values. Never commit .env.
# Full stack config lives in ../kodex-infra/.env.example

# MODULE_CONTRACT: Document the frontend runtime environment defaults.
# SCOPE: Backend URL defaults consumed by the Vite frontend shell.
# DEPENDS: package.json, vite.config.ts, src/env.d.ts
# LINKS: [FrontendTooling][setup][BLOCK_DEFINE_TOOLCHAIN]
# START_BLOCK_ENV_DEFAULTS
VITE_API_BASE_URL=http://localhost:8000
VITE_AGENT_SEARCH_URL=http://localhost:8000/v1/agent/search
# END_BLOCK_ENV_DEFAULTS
